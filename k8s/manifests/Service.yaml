apiVersion: v1
kind: Service
metadata:
  name: devhub-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: devhub-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 31214